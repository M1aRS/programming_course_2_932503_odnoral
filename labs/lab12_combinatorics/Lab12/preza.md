# Иллюстрация алгоритма генерации перестановок
## Лабораторная работа №11
### Мощность множества n = 3

---

### Исходный код

```cpp
№1 #include <iostream>
№2 using namespace std;

№3 int *P, *R, n;

№4 void per(int k) {
№5  for (int i = 1; i <= n; i++) {
№6      if (R[i] == 0) { 
№7          P[k] = i; R[i] = 1;
№8          
№9          if (k == n) { 
№10              for (int j = 1; j <= n; j++) cout << P[j] << " ";
№11              cout << endl;
№12          } else per(k + 1);          
№13          R[i] = 0;          
№14      }
№15  }
№16 }

№17 int main() {
№18     n = 3;
№19     P = new int[n + 1]; 
№20     R = new int[n + 1]{0};
№21     per(1);
№22     return 0;
№23 }
```

---

### Шаг 0: Выделение места под переменные-указатели
Выделяем память на переменные.
```cpp
№3 int *P, *R, n;
```
| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 0 | | |
| **R** = nullptr| | |
| **P** = nullptr | | |

---

### Шаг 1: Объявление переменных
Выделяем память на переменные.

```cpp
№18     n = 3;
№19     P = new int[n + 1]; 
№20     R = new int[n + 1]{0};
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | |
| **R** = (адрес)| **R** = [0,0,0,0] | |
| **P** = (адрес) |  **P** = [?,?,?,?] | |

---

### Шаг 2: Вызов функции
Мы заходим в функцию per(1). В Стек сразу же падают параметр k = 1 и локальная переменная i = 1.

```cpp
№21     per(1);
// --- Внутри per(1) ---
№5  for (int i = 1; i <= n; i++) { 
№6      if (R[i] == 0) {           // R[1] == 0 
№7          P[k] = i; R[i] = 1;    // P[1] = 1, R[1] = 1
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | k = 1, i = 1|
| **R** = (адрес)| **R** = [0,1,0,0] | |
| **P** = (адрес) |  **P** = [?,1,?,?] | |

---

### Шаг 3: Вызов функции. Рекурсия
Функция per(1) вызывает per(2).

```cpp
№12          } else per(k + 1);    // Вызов per(2)
// --- Внутри per(2) ---
№5  for (int i = 1; i <= n; i++) { // i=1 занято, берем i=2
№7          P[k] = i; R[i] = 1;    // P[2] = 2, R[2] = 1
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | k = 2, i = 2 |
| **R** = (адрес)| **R** = [0,1,1,0] | k = 1, i = 1|
| **P** = (адрес) |  **P** = [?,1,2,?] | |

---

### Шаг 4: Вызов функции. Вывод.
Функция per(2) вызывает per(3). Происходит печать.

```cpp
№12          } else per(k + 1);    // Вызов per(3)
// --- Внутри per(3) ---
№5  for (int i = 1; i <= n; i++) { // берем свободное i=3
№7          P[k] = i; R[i] = 1;    // P[3] = 3, R[3] = 1
№9          if (k == n) {          // 3 == 3 
№10              ... cout << P[j]; // ВЫВОД: 1 2 3
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | |   k = 3, i = 3|
| **R** = (адрес)| **R** = [0,1,1,1] |k = 2, i = 2 |
| **P** = (адрес) |  **P** = [?,1,2,3] | k = 1, i = 1|

---

### Шаг 5: Бэктрекинг. Откат из per(3).
После вывода мы делаем откат: освобождаем R[3] = 0. Функция per(3) завершается, и её блок переменных навсегда удаляется из Стека.

```cpp
№13         R[i] = 0;              // ОТКАТ: R[3] = 0
№14     }
№15  }                             // Выходим из per(3)
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | |  ~~k = 3, i = 3~~|
| **R** = (адрес)| **R** = [0,1,1,0] |k = 2, i = 2 |
| **P** = (адрес) |  **P** = [?,1,2,3] | k = 1, i = 1|

---

### Шаг 6: Продолжение вычисление перестановок.
Управление вернулось в per(2). Сначала мы делаем откат для текущего i=2. Затем цикл for идет дальше и i становится 3. Стек обновляется.

```cpp
// --- Вернулись в per(2) ---
№13         R[i] = 0;              // ОТКАТ: освобождаем R[2]
            |
            V
№5  for (int i = 1; i <= n; i++) { // Цикл делает шаг! i = 3
№7          P[k] = i; R[i] = 1;    // Пишем P[2] = 3, R[3] = 1
№12         ... else per(k + 1);   // Снова вызываем per(3)
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | k = 2, i = 3 |
| **R** = (адрес)| **R** = [0,1,0,1] | k = 1, i = 1 |
| **P** = (адрес) |  **P** = [?,1,3,3] | |

---

### Шаг 7: Снова per(3).
Вновь зашли в per(3). В Стек ложится новый кадр. Ищем свободное число (это 2).

```cpp
// --- Зашли в per(3) ---
№5  for (int i = 1; i <= n; i++) { // Свободно только i=2
№7          P[k] = i; R[i] = 1;    // P[3] = 2, R[2] = 1
№9          if (k == n) {          // 3 == 3
№10              ... cout << P[j]; // ВЫВОД: 1 3 2
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | k = 3, i = 2 |
| **R** = (адрес)| **R** = [0,1,1,1] | k = 2, i = 3 |
| **P** = (адрес) |  **P** = [?,1,3,2] | k = 1, i = 1  |

---

### Шаг 8: Бэктрекинг. 
Вывели 1 3 2. Теперь функция per(3) делает откат (освобождает число 2) и завершается. Кадр удаляется из Стека.

```cpp
// --- Продолжение в per(3) ---
№13         R[i] = 0;              // ОТКАТ: освобождаем R[2] = 0
№14     }
№15  }                             // Конец цикла (i=2), переходим к i=3. Для i=3 R[3]==1, цикл завершается. Выход из per(3).
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | ~~k = 3, i = 2~~ |
| **R** = (адрес)| **R** = [0,1,0,1] | k = 2, i = 3 |
| **P** = (адрес) |  **P** = [?,1,3,2] | k = 1, i = 1  |

---

### Шаг 9: Глубокий бэктрекинг. 
Управление вернулось в per(2). Мы должны сделать откат для i=3. Затем цикл for в per(2) пытается пойти дальше, но предел n=3 уже достигнут. Значит, per(2) тоже завершает работу и удаляется из Стека.

```cpp
// --- Вернулись в per(2) ---
№13         R[i] = 0;              // ОТКАТ: освобождаем R[3] = 0
№14     }
№15  }                             // Цикл окончен (i достигло 3). Выход из per(2).
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | ~~k = 2, i = 3~~ |
| **R** = (адрес)| **R** = [0,1,0,0] | k = 1, i = 1  |
| **P** = (адрес) |  **P** = [?,1,3,2] |  |

---

### Шаг 10: Смена первой цифры (Продолжение в per(1)) 
Мы вернулись в самое начало, в per(1). Делаем откат для i=1. И теперь цикл for берет новую стартовую цифру 2.
```cpp
// --- Вернулись в per(1) ---
№13         R[i] = 0;              // ОТКАТ: освобождаем R[1] = 0. Теперь R полностью чист! [0,0,0,0]
№5  for (int i = 1; i <= n; i++) { // Шаг цикла: переходим к i = 2
№7          P[k] = i; R[i] = 1;    // Пишем P[1] = 2, R[2] = 1
№12         ... else per(k + 1);   // Вызываем per(2) для новой ветки
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | k = 1, i = 2 |
| **R** = (адрес)| **R** = [0,0,1,0] |  |
| **P** = (адрес) |  **P** = [?,2,3,2] |  |

---

### Шаг 11: Снова per(2) (Выбор второй цифры) 
В стек ложится новый кадр per(2). Ищем свободное число с самого начала. Единица свободна! Берем её.
```cpp
// --- Зашли в per(2) ---
№5  for (int i = 1; i <= n; i++) { // Проверяем i=1
№6      if (R[i] == 0) {           // R[1] == 0 (Свободно!)
№7          P[k] = i; R[i] = 1;    // P[2] = 1, R[1] = 1
№12         ... else per(k + 1);   // Вызываем per(3)
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | k = 2, i = 1 |
| **R** = (адрес)| **R** = [0,1,1,0] | k = 1, i = 2 |
| **P** = (адрес) |  **P** = [?,2,1,2] |  |


---

### Шаг 12: Снова per(3) (Получение третьей комбинации)
Зашли в per(3). Ищем свободное число. 1 и 2 заняты. Свободна только тройка. Записываем её и выводим третью комбинацию.
```cpp
// --- Зашли в per(3) ---
№5  for (int i = 1; i <= n; i++) { // i=1,2 заняты. i=3 СВОБОДНО!
№7          P[k] = i; R[i] = 1;    // P[3] = 3, R[3] = 1
№9          if (k == n) {          // 3 == 3
№10              ... cout << P[j]; // ВЫВОД НА ЭКРАН: 2 1 3
```

| Статическая  | Динамическая | Стек (Вызовы) |
| :--- | :--- | :--- |
| **n** = 3 | | k = 3, i = 3 |
| **R** = (адрес)| **R** = [0,1,1,1] | k = 2, i = 1 |
| **P** = (адрес) |  **P** = [?,2,1,3] | k = 1, i = 2 |

---

### Конец демонстрации
Цель достигнута. Получены перестановки:
1. **123**
2. **132**
3. **213**
