# Иллюстрация алгоритма генерации перестановок
## Лабораторная работа №9 (Упражнение 1)
### Мощность множества n = 3

---

### Исходный код

```cpp
#include <iostream>


using namespace std;


int* currentPermutation;
int* isNumberUsed;
int totalElements;

void printResult() {
    for (int i = 0; i < totalElements; i++) {
        cout << setw(3) << currentPermutation[i];
    }
    cout << endl;
}


void generatePermutations(int currentPos) {
    for (int num = 1; num <= totalElements; num++) {
        if (isNumberUsed[num] == 0) {
            currentPermutation[currentPos] = num;
            isNumberUsed[num] = 1;

            if (currentPos == totalElements - 1) {
                printResult();
            } 
            else {
                generatePermutations(currentPos + 1);
            }
            isNumberUsed[num] = 0;
        }
    }
}

int main() {
    if (!(cin >> totalElements) || totalElements <= 0) {
        cout << "Ошибка ввода!" << endl;
        return 1;
    }


    currentPermutation = new int[totalElements];
    isNumberUsed = new int[totalElements + 1];

    for (int i = 0; i < totalElements; i++) currentPermutation[i] = 0;
    for (int i = 0; i <= totalElements; i++) isNumberUsed[i] = 0;

    cout << "Список всех перестановок для n = " << totalElements << ":" << endl;
    
    generatePermutations(0);

    delete[] currentPermutation;
    delete[] isNumberUsed;

    return 0;
}
```

---

### Шаг 1: Начальное состояние
**main** вызывает `per(1)`

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **main** |
| **P** = [?, ?, ?] | |
| **R** = [0, 0, 0] | |

---

### Шаг 2: per(1), i = 1
Занимаем число **1** для 1-й позиции. Вызов `per(2)`.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(1)** (i=1) |
| **P** = [**1**, ?, ?] | **main** |
| **R** = [**1**, 0, 0] | |

---

### Шаг 3: per(2), i = 2
Число 1 занято (R[1]=1). Берем **2** для 2-й позиции. Вызов `per(3)`.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(2)** (i=2) |
| **P** = [1, **2**, ?] | **per(1)** (i=1) |
| **R** = [1, **1**, 0] | **main** |

---

### Шаг 4: per(3), i = 3
Числа 1 и 2 заняты. Берем **3**. 
**k = n (3 = 3)** — Достигнуто условие вывода.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(3)** (i=3) |
| **P** = [1, 2, **3**] | **per(2)** (i=2) |
| **R** = [1, 1, **1**] | **per(1)** (i=1) |

**ВЫВОД: 1 2 3**

---

### Шаг 5: Возврат (Backtracking) в per(2)
Завершили `per(3)`, обнулили `R[3]`. В `per(2)` цикл `i` идет дальше. i = 3.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(2)** (i=3) |
| **P** = [1, **3**, ?] | **per(1)** (i=1) |
| **R** = [1, 0, **1**] | **main** |

*Заметьте: R[2] теперь 0, а R[3] стал 1.*

---

### Шаг 6: per(3), i = 2
Вызов из Шага 5. Числа 1 и 3 заняты. Свободно только **2**.
**k = n (3 = 3)**.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(3)** (i=2) |
| **P** = [1, 3, **2**] | **per(2)** (i=3) |
| **R** = [1, **1**, 1] | **per(1)** (i=1) |

**ВЫВОД: 1 3 2**

---

### Шаг 7: Откат к самому началу per(1)
Все варианты, начинающиеся на "1", закончены. В `per(1)` цикл `i` переходит к **2**.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(1)** (i=2) |
| **P** = [**2**, ?, ?] | **main** |
| **R** = [0, **1**, 0] | |

---

### Шаг 8: per(2), i = 1
Число 2 занято. Свободно число **1**. Вызов `per(3)`.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(2)** (i=1) |
| **P** = [2, **1**, ?] | **per(1)** (i=2) |
| **R** = [**1**, 1, 0] | **main** |

---

### Шаг 9: per(3), i = 3
Числа 2 и 1 заняты. Берем **3**.
**k = n (3 = 3)**.

| Статическая память | Стек (Вызовы) |
| :--- | :--- |
| **n** = 3 | **per(3)** (i=3) |
| **P** = [2, 1, **3**] | **per(2)** (i=1) |
| **R** = [1, 1, **1**] | **per(1)** (i=2) |

**ВЫВОД: 2 1 3**

---

### Конец демонстрации
Цель достигнута. Получены перестановки:
1. **123**
2. **132**
3. **213**

Программа продолжит работу аналогично для i = 3 в `per(1)`.
